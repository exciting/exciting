include ../make.inc

FC = $(F90)
FFLAGS = $(F90_OPTS) $(CPP_ON_OPT)
LD = $(FC)
LDFLAGS = $(F90_OPTS) $(LIBS) libmsec.a   libfox.a
AR = ar
ARFLAGS = -rc
F77=$(F77) $(F77_OPTS)
TMPFILES = *.mod
SUFFIX=ser
