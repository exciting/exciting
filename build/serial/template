include make.inc

FC = $(F90)
### default
FFLAGS = $(F90_OPTS) $(F90CPP_OPTS)
### debug
#FFLAGS = $(F90_OPTS) -check all -traceback
LD = $(FC)
LDFLAGS = $(LIB_LPK) $(LIB_FFT) $(LIB_SYS) $(LIB_BZINT) libarpack.a libjdqz.a
AR = ar
ARFLAGS = -rc

TMPFILES = *.mod

