include ../make.inc

FC = $(F90MT) $(CPP_ON_OPT)
F90=$(FC)
F77=$(F77MT)
### default
FFLAGS = $(SMPF90_OPTS) 
LD = $(FC)
LDFLAGS = $(SMPF90_OPTS)  $(SMP_LIBS) libmsec.a
F77_OPTS =$(SMPF77_OPTS)
TMPFILES = *.mod
SUFFIX=smp
