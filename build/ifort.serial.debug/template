include make.inc

FC = $(F90)

FFLAGS = $(F90_OPTS) $(CPP_ON_OPT)
LD = $(FC)
LIBS = $(LIB_LPK) $(LIB_FFT) $(LIB_BZINT) $(LIB_SYS)
LDFLAGS = $(LIBS) libmsec.a
AR = ar
ARFLAGS = -rc
F77_OPTS =$(F77_OPTS)
TMPFILES = *.mod
SUFFIX=debug
