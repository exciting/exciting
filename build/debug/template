include ../make.inc

FC = $(F90)

FFLAGS = $(F90_DEBUGOPTS) $(CPP_ON_OPT)
LD = $(FC)
LDFLAGS = $(LIBS) $(F90_DEBUGOPTS)  libmsec.a    libfox.a
F77_OPTS =$(F77_DEBUGOPTS)
TMPFILES = *.mod
SUFFIX=debug
