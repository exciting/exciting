include ../make.inc

FC = $(MPIF90MT) $(CPP_ON_OPT)
FFLAGS = $(MPISMPF90_OPTS)
LD = $(FC)
LDFLAGS = $(MPISMPF90_OPTS) $(SMP_LIBS) libmsec.a
F77_OPTS =$(SMPF77_OPTS)
F77=$(F77MT)
TMPFILES = *.mod
SUFFIX=mpismp