include ../../build/make.inc

FC = $(F90) -I ../../src/FoX/objs/finclude


FFLAGS = $(F90_DEBUGOPTS) $(CPP_ON_OPT)

LD = $(FC)
LDFLAGS =$(F90_OPTS) $(LIBS) $(F90_DEBUGOPTS) libmsec.a libfox.a
F77=$(F77) 

TMPFILES = *.mod
